# ========================================
# ExFlow 経費管理システム 環境変数設定例
# このファイルを .env にコピーして使用してください
# ========================================

# JWTトークン署名用シークレットキー
# ※ 本番環境では必ず長くランダムな文字列に変更してください
# 生成方法: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this-in-production-use-random-string

# JWTトークン有効期限 (分)
ACCESS_TOKEN_EXPIRE_MINUTES=480

# サーバーポート番号 (Railway等では自動設定されます)
PORT=8000

# データベースURL (SQLite: ファイルパス / PostgreSQL: 接続文字列)
# SQLite (デフォルト・ローカル開発向け)
DATABASE_URL=sqlite:///./expflow.db
# PostgreSQL (本番環境向け・Railway PostgreSQLプラグイン使用時)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# LINE Messaging API設定 (LINE連携を使用する場合)
# LINE Developersコンソールで取得してください
LINE_CHANNEL_SECRET=your-line-channel-secret
LINE_CHANNEL_ACCESS_TOKEN=your-line-channel-access-token

# アプリケーションのベースURL (本番環境のドメインに設定)
APP_BASE_URL=https://your-app.railway.app

# 管理者初期パスワード (起動時の初期データ生成に使用)
ADMIN_INITIAL_PASSWORD=admin1234

# アップロードファイル保存先ディレクトリ
UPLOAD_DIR=./uploads
